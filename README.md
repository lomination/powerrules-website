# PowerRules Website

Repository for PowerRules compiler: [github.com/lomination/powerrules](https://github.com/lomination/powerrules).

## Run

Install the dependencies with `npm install`.

You can open a preview on local machine or network using

```shell
npm run dev # add '-- --host' for local network
```

Or build it using

```shell
npm run build
```